export default class Product {
    name;
    price;
    category;

    constructor(name, category, price) {
        this.name = name;
        this.price = price;
        this.category = category;
    }
}